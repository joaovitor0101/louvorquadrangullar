// Serviço de IA removido.
// Este arquivo agora está vazio para garantir que o build não falhe se houver referências residuais, 
// mas a lógica foi movida para entrada manual no AdminPanel.
export {};